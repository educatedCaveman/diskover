version: '3'

networks:
  diskover:

services:

  diskover:
    image: lscr.io/linuxserver/diskover@sha256:bc39a2b814b92440c8fba6cce36531b4c04fdf700772bedfa4ff0660150b12e9
    container_name: diskover
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=America/New_York
      - ES_HOST=elasticsearch
      - ES_PORT=9200
    volumes:
      - /docker/docker-compose/diskover/config:/config
      - /:/data
    ports:
      - 8888:80
    restart: unless-stopped
    depends_on:
      - elasticsearch
    networks:
      - diskover

  elasticsearch:
    container_name: elasticsearch
    image: docker.elastic.co/elasticsearch/elasticsearch:8.10.4
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
      - bootstrap.memory_lock=true
      - "ES_JAVA_OPTS=-Xms1g -Xmx1g"
    ulimits:
      memlock:
        soft: -1
        hard: -1
    volumes:
      - /docker/docker-compose/diskover/elasticsearch_data:/usr/share/elasticsearch/data
    ports:
      - 9200:9200
      - 9300:9300
    restart: unless-stopped
    networks:
      - diskover
